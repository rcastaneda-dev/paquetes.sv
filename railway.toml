[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
# CRITICAL: Set working directory to worker folder
workingDirectory = "worker/zip-worker"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
