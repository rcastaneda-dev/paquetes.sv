[build]
builder = "DOCKERFILE"
# Use Dockerfile designed for repo root context
dockerfilePath = "Dockerfile.worker"
# Only rebuild when worker files change
watchPaths = ["worker/zip-worker/**", "Dockerfile.worker"]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
